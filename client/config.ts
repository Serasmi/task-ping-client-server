export const checkedServer = {
  url: "https://fundraiseup.com",
  delay: 1_000, // ms
};

export const retryOptions = {
  maxAttempts: 5,
  growFactor: 100, // ms
};

export const reportServer = {
  hostname: "localhost",
  port: 8080,
  timeout: 10_000, // ms
};
